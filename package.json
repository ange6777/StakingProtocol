{
  "name": "staking-protocol",
  "version": "1.0.0",
  "description": "A decentralized staking protocol with deposits, rewards, and withdrawals",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "node": "hardhat node",
    "dev": "npm run compile && npm run node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "keywords": ["staking", "defi", "ethereum", "solidity"],
  "author": "",
  "license": "MIT"
}
